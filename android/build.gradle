// ------------------------------------------------------------
// Root-level build.gradle
// ------------------------------------------------------------

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Only needed if you have legacy buildscript dependencies
        classpath "com.android.tools.build:gradle:8.1.1"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.10"
    }
}

// Use the plugins block with version numbers for Gradle 8.x
plugins {
    id 'com.android.application' version '8.1.1' apply false
    id 'com.android.library' version '8.1.1' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.10' apply false
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

// Optional clean task
tasks.register("cleanBuild") {
    group = "build"
    description = "Cleans all build directories"
    doLast {
        delete fileTree(dir: "build")
        println "Root build directories cleaned."
    }
}